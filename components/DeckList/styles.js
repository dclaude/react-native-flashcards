import { StyleSheet } from 'react-native';
import { white, lightGray } from '../../styles/common'

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: white,
    alignItems: 'stretch',
    justifyContent: 'center',
  },
  separator: {
    height: 2,
    backgroundColor: lightGray,
  },
})

